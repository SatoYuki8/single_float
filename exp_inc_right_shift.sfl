circuit exp_inc_right_shift{
    input exp<8>;
    input frac<26>;
    output inc_exp<8>;
    output shifted_frac<26>;
    instrin do;

    instruct do par{
	inc_exp = exp + 0b00000001;
	shifted_frac = frac >> 0b1;
    }
}
